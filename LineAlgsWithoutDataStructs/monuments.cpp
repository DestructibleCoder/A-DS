#include <iostream>

using std::cout, std::endl, std::cin;

int main() {
  int n, l;
  cin >> n >> l;

  int arr[n];
  for (int i = 0; i < n; i++)
    cin >> arr[i];

  for (int i = 0; i < n; i++) {
  }
}
