#include <iostream>
#include <string>
#include <vector>

using namespace std;

int main() {
  int n, m;
  cin >> n >> m;

  string s;
  cin >> s;

  vector<vector<short>> dp(n + 1, vector<short>(n + 1, -1));
}
